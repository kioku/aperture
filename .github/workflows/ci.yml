
name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Run checks
        run: |
          cargo fmt --check
          cargo clippy --no-deps -- -D warnings
          cargo test
